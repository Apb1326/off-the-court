# Off the Court — Roadmap revision history

> Moved verbatim from the `docs/ROADMAP.md` header on 2026-07-11 (only the blockquote
> prefix was stripped). This file is **background, not required reading** for executing a
> phase: it preserves the reasoning trail of how the plan reached its current shape.
> `docs/ROADMAP.md` remains the sole owner of current statuses, sequencing, and phase
> specs; new revision notes are appended here (newest first) when the roadmap changes.

**Revision 8 changes (current-state and execution-contract truth-up, 2026-07-10):** corrects the post-F1 schema-v7/controlled-team status in current-state and active phase text, records S2a/S2b/S2b-R without treating the candidate as active, splits the former oversized S2c into candidate-tendencies **S2c1** and assist/compensation **S2c2**, and makes S2d the sole activation point. It also distinguishes F2's immediate readiness from S3's S2d gate; makes S3's lineup-validation harness the first Stage-3 unit; upgrades T-5c from one fixed world to a paired fixed-seed suite; settles the v1 valuation information boundary; aligns T-7's persisted inbox events with U1a's UI; and splits T-8 around lottery/order, assets, prospects, and draft execution. This is a docs-only revision: no runtime behavior changed.

**Revision 7 changes (R0b post-repair housekeeping, 2026-07-06):** records the accepted S1-R result — S1-Ra (PR #20, commit 8c27361) moved seed selection/validation to the API boundary with byte-identical fixed-seed output, and S1-Rb (PR #21, commit acc011f) repaired average margin **16.8 → 13.4 vs 12.87 ± 1.0** via a deterministic, bounded, symmetric coasting/effort response (`COAST_LEAD_START`/`COAST_LEAD_FULL`/`COAST_SHOT_EFFORT_MAX`), bringing `npm run profile` to **PASS (32/32 ENFORCED, exit 0)**. The assisted-zone sign structure was **diagnosed and handed off** to its S2/S3 owner (routing correct; the gap is definitional — see §4.1a outcome). The historical acceptance-failure record in §1.0 and §4.1 is preserved as history, not erased. Also: `docs/prompts/` created (legacy `PHASE_*` prompts moved; the finalized Stage 1 prompt committed with its divergence/repair annotation), the shipped seed boundary codified in `AGENTS.md`, the stale `shouldDoubleTeam` comment fixed, and README refreshed.

**Revision 6 changes (execution-readiness review, 2026-07-06):** (1) a docs-only **R0a** truth-up moves before S1-R so a fresh agent can discover this roadmap and the implemented Phase-5b/schema-v6 state without waiting for an unrelated engine repair; post-repair wording remains **R0b**. (2) S1-R explicitly audits the existing garbage-time substitution rule, removes ambient `Date.now()` from `simulateSeason`, validates/generates seeds at the API boundary, and requires two reviewable commits around its behavior-preserving checkpoint. (3) S2, F4, and T-7/U1 become ordered subphases rather than oversized single diffs. (4) F3 creates the fresh next-season base before calling the existing rollover API. (5) F4 acceptance profiles evolved year-3/year-5/year-10 pools instead of proving only year-1 invariance. (6) T-6 separates bounded value imbalance, asset-universe conservation, and sequence-level value flow; a transfer is no longer mislabeled as value creation. (7) every prompt captures pre-change baselines before editing and records the sandbox-safe `node --import tsx` fallback.

**Revision 5 changes (execution-contract cleanup, 2026-07-06):** (1) S1-R now requires the seed-boundary refactor to be applied and verified as an **intermediate behavior-preserving checkpoint before margin tuning begins**; only that checkpoint is byte-identical to the pre-repair engine, while the final margin repair is expected to change fixed-seed outcomes and is judged by repeat-run determinism. (2) The current `AGENTS.md` Pareto rule remains universal: every game-facing executed trade, including a controlled-team proposal, passes the context-free base-value referee outside the legality gate; CPU desirability remains the controlled team's gameplay defense on top of that invariant. (3) F3 rotation repair is explicitly two-stage: sanitize stale membership immediately after rollover, then fill starters and rebalance minutes only after autofill restores the roster. (4) The incomplete modern-era `calibrate` rider is removed from S1-R; a complete modern benchmark requires its own normalized-games derivation and remains in Horizon.

**Revision 4 changes (maintainer review pass, 2026-07-06):** (1) **S1's status is corrected from "shipped" to "implemented, acceptance failing"** — the live profile fails average margin (16.8 vs 12.87 ± 1.0) and the assisted-zone sign-structure sanity check (corner three is not the highest-assisted zone in engine output). Per `AGENTS.md` and this document's own rule 3, an engine change failing calibration is unfinished; a repair phase **S1-R** (§4.1a) is inserted as the immediate next work, ahead of R0 and F1. (2) **The seed-selection "exception" is repealed** — `createSeasonState`'s `Math.random()` default violates the `src/engine` determinism rule as written; the fix is architectural (seed chosen at the menu/API boundary, engine requires it), not a policy carve-out (§9.2, executed in S1-R). (3) **A shared deterministic rotation-repair primitive moves up to F3** — rollover/autofill (F3) and retirement (F4) mutate rosters before F5 exists, and `processContractRollover` removes players without repairing persisted rotations; F5 keeps stat stints and deadline wiring (§5.3/§5.4/§5.5, new invariant §9.10). (4) **S2 and F4 gain explicit statistical contracts** — distributional requirements (spread, tails, shrinkage, correlations) for the rating mapping, and proxy-mapping tables with coverage windows plus held-out validation for the aging curves (§4.2, §5.4). (5) **`npm run calibrate` is reclassified** — its historical data ends in 2015, so against a 2023–26-tuned engine it is a deterministic drift comparison, not an era-fidelity acceptance ("sane by era" is retired from every checklist; §0, §10, Appendix B). (6) **The Pareto referee's claims are narrowed and the sequence checks made normative** — the per-trade check applies to context-free base value only (fit-adjusted mutual gains are legitimate gains from trade), and the 5c N-season sequence metrics (cumulative net base-value flow, cycle detection) are the normative anti-laundering acceptance (§6.1/§6.2).

**Revision 3 changes (retained):** §4.1 converted to a shipped-contract record with three written-back divergences — profile-as-acceptance replacing the BASE oracle; the full six-zone re-map (short/long midrange split at 14 ft); assisted rates demoted to informational with the box assist total as the enforced chain anchor — plus the documented `PLAY_TYPE_SHOT_ZONES` shading as a named S2 unwind duty. **Revision 2 changes (retained):** F5 ahead of the harness; F4 replacement-level generation; careerStats provenance; seed lineage; Pareto placement outside the gate; era-consistency; survivor-bias/era-normalization requirements; ∥ = sequential merges.
